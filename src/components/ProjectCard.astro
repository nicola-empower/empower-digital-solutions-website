---
import { ArrowRight } from "lucide-react";
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

interface Props {
  title: string;
  category: string;
  href: string;
  image: ImageMetadata;
  linkText?: string;
}

const { title, category, href, image, linkText = "View Project" } = Astro.props;
---

<a
  href={href}
  target={href.startsWith("http") ? "_blank" : "_self"}
  rel={href.startsWith("http") ? "noopener noreferrer" : ""}
  class="group block bg-white rounded-xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 hover:-translate-y-2"
>
  <div class="relative aspect-video overflow-hidden bg-gray-200">
    <Image
      src={image}
      alt={title}
      width={600}
      class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
    />
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-6"
    >
      <span class="text-white font-bold flex items-center gap-2">
        {linkText}
        <ArrowRight className="w-4 h-4" />
      </span>
    </div>
  </div>
  <div class="p-6">
    <p
      class="text-sm font-bold text-empower-pink mb-2 uppercase tracking-wider"
    >
      {category}
    </p>
    <h3
      class="text-xl font-bold text-deep-purple group-hover:text-empower-pink transition-colors"
    >
      {title}
    </h3>
  </div>
</a>
