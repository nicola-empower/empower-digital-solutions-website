---
import "../../styles/main.css"; // Ensure Tailwind is loaded
import {
  ShieldCheck,
  Server,
  AlertTriangle,
  Mail,
  Printer,
} from "lucide-astro";

const date = new Date().toLocaleDateString("en-GB", {
  year: "numeric",
  month: "long",
});
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Domain Migration Protocol</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body
    class="bg-slate-100 text-slate-900 font-sans print:bg-white web-preview min-h-screen flex flex-col items-center py-10 print:py-0"
  >
    <!-- Floating Print Button (Hidden in Print) -->
    <button
      onclick="window.print()"
      class="fixed bottom-8 right-8 bg-purple-600 hover:bg-purple-700 text-white font-bold py-4 px-6 rounded-full shadow-xl z-50 flex items-center gap-2 transition-all hover:scale-105 print:hidden"
    >
      <Printer class="w-5 h-5" />
      Print Guide
    </button>

    <main
      class="w-full max-w-[210mm] min-h-[297mm] p-[15mm] md:p-[20mm] bg-white relative shadow-2xl print:shadow-none print:w-full print:max-w-none print:m-0"
    >
      <header
        class="flex justify-between items-start border-b-4 border-purple-900 pb-8 mb-12"
      >
        <div>
          <h1
            class="text-3xl font-extrabold text-slate-900 mb-2 uppercase tracking-wide"
          >
            Migration<span class="text-purple-600">Protocol</span>
          </h1>
          <p class="text-sm text-slate-500 font-mono tracking-widest">
            OFFICIAL CLIENT DOCUMENTATION
          </p>
        </div>
        <div class="text-right">
          <div class="text-sm font-bold text-slate-900">
            Empower Digital Solutions
          </div>
          <div class="text-xs text-slate-500">{date}</div>
          <div class="text-xs text-purple-600 font-mono mt-1">
            DOC_REF: DNS_MIG_v1
          </div>
        </div>
      </header>

      <section class="mb-12">
        <h2
          class="text-xl font-bold text-slate-800 mb-4 flex items-center gap-2"
        >
          <ShieldCheck class="w-6 h-6 text-purple-600" />
          1. The "Zero-Risk" Guarantee
        </h2>
        <div
          class="bg-slate-50 p-6 rounded-lg border-l-4 border-purple-600 text-sm leading-relaxed text-slate-700"
        >
          <p class="mb-4">
            <strong>You are not losing your domain.</strong> Moving to a new website
            does not mean starting over. Your Google Reviews, SEO history, and Email
            archives are safe.
          </p>
          <p>
            <strong>The Analogy:</strong> Think of this like upgrading your mobile
            phone. We are simply swapping the "SIM Card" (The DNS Records) to a new,
            faster device (The Vercel Server). Your phone number (Domain Name) and
            Contacts (Email) stay exactly the same.
          </p>
        </div>
      </section>

      <section class="mb-12">
        <div
          class="bg-red-50 border border-red-100 rounded-lg p-6 flex gap-4 items-start"
        >
          <div class="bg-red-100 p-2 rounded-full text-red-600 mt-1">
            <AlertTriangle class="w-6 h-6" />
          </div>
          <div>
            <h3 class="font-bold text-red-800 text-lg mb-1">
              CRITICAL WARNING: Do Not Touch Email Settings
            </h3>
            <p class="text-sm text-red-700">
              When editing your settings, you will see records labeled <strong
                >MX (Mail Exchanger)</strong
              >.
              <br />
              <strong>NEVER delete or edit these.</strong> These control your email
              inbox. We only touch the "A-Record" (Website).
            </p>
          </div>
        </div>
      </section>

      <section class="mb-12">
        <h2
          class="text-xl font-bold text-slate-800 mb-6 flex items-center gap-2"
        >
          <Server class="w-6 h-6 text-purple-600" />
          2. Configuration Steps
        </h2>

        <div class="space-y-6">
          <div class="border border-slate-200 rounded-lg overflow-hidden">
            <div
              class="bg-slate-100 px-4 py-2 border-b border-slate-200 font-bold text-sm text-slate-700"
            >
              Step 1: Locate DNS Settings
            </div>
            <div class="p-4 text-sm text-slate-600 space-y-2">
              <p>Log into your provider (IONOS, GoDaddy, Wix, etc).</p>
              <p>
                Navigate to <strong>Domains</strong> &rarr; <strong
                  >Manage DNS</strong
                > (or "DNS Zones").
              </p>
            </div>
          </div>

          <div class="border border-slate-200 rounded-lg overflow-hidden">
            <div
              class="bg-slate-100 px-4 py-2 border-b border-slate-200 font-bold text-sm text-slate-700"
            >
              Step 2: Update the 'A-Record'
            </div>
            <div class="p-4 text-sm text-slate-600">
              <p class="mb-3">
                Find the record with Type <strong>A</strong> and Host <strong
                  >@</strong
                >.
              </p>
              <div
                class="bg-slate-900 text-white p-4 rounded font-mono text-xs"
              >
                <div
                  class="grid grid-cols-4 gap-4 mb-2 opacity-50 border-b border-slate-700 pb-2"
                >
                  <span>Type</span><span>Host / Name</span><span
                    >Value / Points To</span
                  ><span>TTL</span>
                </div>
                <div class="grid grid-cols-4 gap-4 font-bold text-green-400">
                  <span>A</span><span>@</span><span>76.76.21.21</span><span
                    >Auto / 1h</span
                  >
                </div>
              </div>
              <p class="mt-3 text-xs italic">
                *Note: If you are using Wix/Squarespace, you may need to delete
                their default records first.
              </p>
            </div>
          </div>

          <div class="border border-slate-200 rounded-lg overflow-hidden">
            <div
              class="bg-slate-100 px-4 py-2 border-b border-slate-200 font-bold text-sm text-slate-700"
            >
              Step 3: Update the 'CNAME'
            </div>
            <div class="p-4 text-sm text-slate-600">
              <p class="mb-3">
                Find (or Add) the record with Type <strong>CNAME</strong> and Host
                <strong>www</strong>.
              </p>
              <div
                class="bg-slate-900 text-white p-4 rounded font-mono text-xs"
              >
                <div class="grid grid-cols-4 gap-4 font-bold text-purple-300">
                  <span>CNAME</span><span>www</span><span
                    >cname.vercel-dns.com</span
                  ><span>Auto</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <footer class="absolute bottom-0 left-0 w-full p-[15mm] md:p-[20mm]">
        <div
          class="border-t border-slate-200 pt-6 flex justify-between items-center"
        >
          <div class="text-xs text-slate-400">
            <p class="font-bold text-slate-600">Need "White Glove" Support?</p>
            <p>We can handle this login process for you securely.</p>
          </div>
          <div class="flex items-center gap-2">
            <Mail class="w-4 h-4 text-purple-600" />
            <span class="text-sm font-bold text-slate-700"
              >nicola@empowerdigitalsolutions.co.uk</span
            >
          </div>
        </div>
      </footer>
    </main>

    <style>
      /* Print-specific tweaks to force colors */
      @media print {
        body {
          background: none;
          -webkit-print-color-adjust: exact !important;
          print-color-adjust: exact !important;
          padding: 0;
        }
        main {
          box-shadow: none;
          margin: 0;
          width: 100%;
          max-width: none;
        }
        /* Hide browser default headers/footers in print dialog settings */
      }
    </style>
  </body>
</html>
