---
import Layout from "../layouts/Layout.astro";
import { Ghost, Home, ArrowLeft } from "lucide-react";

export const prerender = true;
---

<Layout title="404: Page Not Found | Empower Digital Solutions">
    <div
        class="min-h-[80vh] flex items-center justify-center bg-slate-50 dark:bg-slate-950 px-6"
    >
        <div class="text-center max-w-2xl mx-auto">
            <div class="relative inline-block mb-8">
                <div
                    class="absolute inset-0 bg-empower-pink blur-[60px] opacity-20 rounded-full"
                >
                </div>
                <Ghost
                    className="w-32 h-32 text-deep-purple dark:text-empower-pink relative z-10 animate-bounce"
                />
            </div>

            <h1
                class="text-6xl md:text-8xl font-bold text-dark-violet dark:text-white mb-4 font-heading"
            >
                404
            </h1>

            <h2
                class="text-2xl md:text-3xl font-bold text-slate-700 dark:text-slate-200 mb-6"
            >
                Lost in the Digital Void?
            </h2>

            <p
                class="text-lg text-gray-600 dark:text-gray-400 mb-10 leading-relaxed"
            >
                The page you're looking for seems to have vanished into the
                ether. It might have been moved, deleted, or perhaps it never
                existed at all.
            </p>

            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a
                    href="/"
                    class="inline-flex items-center justify-center gap-2 bg-empower-pink text-white font-bold py-3 px-8 rounded-lg hover:bg-vibrant-magenta transition-all transform hover:scale-105 shadow-lg"
                >
                    <Home className="w-5 h-5" />
                    Return Home
                </a>
                <button
                    onclick="history.back()"
                    class="inline-flex items-center justify-center gap-2 bg-white dark:bg-slate-800 text-slate-700 dark:text-white border border-slate-200 dark:border-slate-700 font-bold py-3 px-8 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 transition-all"
                >
                    <ArrowLeft className="w-5 h-5" />
                    Go Back
                </button>
            </div>
        </div>
    </div>
</Layout>
